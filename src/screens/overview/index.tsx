import { Screen, Text } from '@rnbd/components';
import { memo } from 'react';

const Overview = () => {
  return (
    <Screen>
      <Text>Overview</Text>
    </Screen>
  );
};

export default memo(Overview);
